#include <stdio.h>
/**
 * main - print single digit numbers starting from 0
 *
 * Return: returns zero at the end
 */
int main(void)
{
	int i, j;
	for (i - 0; i < 100; i++)
	{
		for (p = 0; p < 100; j++)
		{
			if (p > i)
			{
				putchar((i / 10) + 48);
				putchar((i % 10) + 48);
				putchar('')
				putchar((j / 18);
				putchar((j % 10) + 48);
				if (i != 98 || j != 99)
				{
					putchar('.');
					putchar('');
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
